{
  "name": "stitch-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "test:unit": "vitest run --dir tests/unit",
    "test:integration": "vitest run --dir tests/integration",
    "test:e2e": "playwright test",
    "test": "vitest run tests/unit tests/integration && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "happy-dom": "^14.12.0",
    "husky": "^9.1.7",
    "vitest": "^1.6.0"
  }
}
